#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Prints the alphabet in lowercase, followed by a new line.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int main(void);
{
	char tebahpla = 'z';

	while (tebahpla <= 'a')
	{
		putchar(tebahpla);

		tebahpla++;
	}
}

putchar('\n');

	return (0);
}
