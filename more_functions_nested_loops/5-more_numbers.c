#include "main.h"

void more_numbers(void)
{
	int n
	int i = 1
		for (n >= '0' && n <= '9'; n++)
		{
			for (n >= '0'; n <= '9'; n++)
				_putchar(n)
					if (n >= '9')
						_putchar(i++)
							}
}
